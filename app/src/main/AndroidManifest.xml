<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ldm.practica3.spacecowboy">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/player2inv"
        android:roundIcon="@drawable/player2inv"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectDuality">
        <activity
            android:name="com.ldm.practica3.spacecowboy.activities.MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.ProjectDuality.NoActionBar" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>